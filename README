# NetronCli

NetronCli 基于 [Netron](https://github.com/lutzroeder/netron)，
提供命令行能力把深度学习模型解析成 LLM 更易消费的结构化 JSON。

## 项目能做什么

- 检查模型基础信息（`inspect`）
- 导出模型结构（`graph.json`）
- 导出参数索引（`weights_manifest.json`）
- 为“结构先读、参数按需读”的 LLM 工作流提供标准输出

## 快速开始

```bash
node ./bin/netron-cli.mjs --help
node ./bin/netron-cli.mjs inspect <model-path> --json
node ./bin/netron-cli.mjs export all <model-path> --out-dir out/
```

## 文档

- 二进制构建与分发：[`docs/BINARY_BUILD_AND_RELEASE.md`](docs/BINARY_BUILD_AND_RELEASE.md)
- 二进制使用：[`docs/BINARY_USAGE.md`](docs/BINARY_USAGE.md)
